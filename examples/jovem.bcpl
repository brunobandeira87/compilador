GLOBAL LET BOOL y = 5==5;

LET VOID main() BE{

	//LET BOOL y =  TRUE == TRUE;
	//LET INT y =  (4 * 1 * 2 + 1) * 5 - TRUE;
	
}

AND BOOL f1(INT a, INT b, INT c) = VALOF{
	
	RESULTIS y;
}
AND INT f2(INT z, BOOL h) = VALOF {
	LET BOOL e = TRUE;
	e = CALL f1(5,2*2,3+1);
	RESULTIS 3 * 4 * 3;
}
